function setup() {
    createCanvas(500, 400);
   }
  
  function draw() {
    background(imagemdaestrada);
     mostraator();
     mostracarro();
     movimentacarro();
     movimentaator();
     voltaposicaoinicial();
     verificacolisao();
     incluipontos();
     marcaponto();
  }
  
  
  
  